#include <stdio.h>
#include <string.h>
int main(){
    char str1[10];
    gets(str1); //The entered string is stored in str!
    // printf("%s",str1);
    // printf(" Cpmputer Enginnear...");

    puts(str1); // stor str & also print it & also couser in next line(\n)...
    printf("computer Engeinner...");
    return 0;
}